# 项目接力知识库 - 2026-06-06

## 项目位置

- 工作目录：`/Users/longwishine/Documents/代客理财计划`
- iOS 项目：`/Users/longwishine/Documents/代客理财计划/PortfolioDashboardApp/PortfolioDashboardApp.xcodeproj`
- Android 项目：`/Users/longwishine/Documents/代客理财计划/AndroidPortfolioDashboard`
- 最新 Android APK：`/Users/longwishine/Documents/代客理财计划/AndroidPortfolioDashboard/build/PortfolioDashboard-debug.apk`
- HTML 视觉参考：`/Users/longwishine/Documents/代客理财计划/李易凡数据看板_副本.html`

## 用户核心要求

- iOS 和 Android 功能要求保持一致。
- Android 版需要按 Google Material You 视觉语言重写 UI，参考用户提供的三张 iOS 截图。
- 功能不要丢：腾讯行情、持仓、穿透、总资产走势、市场天气、行情日志、手动计算都要保留。
- 输出 APK 前必须检查需求是否完成。

## iOS 已知状态

- SwiftUI 项目位于 `PortfolioDashboardApp/PortfolioDashboardApp.xcodeproj`。
- 已完成过的 iOS 需求包括：
  - 腾讯行情抓取规则可复制到 SMH、QQQM、NVDA、TSM、MU、三大指数和 ETF 穿透标的。
  - 行情日志只保留最近两条。
  - 持仓明细移到第二个 Tab 顶部。
  - 明细页总资产穿透视图下方有“穿透持仓汇总”，合并 SMH / QQQM 同类项，并显示涨跌幅。
  - 总资产走势根据新抓取行情自我迭代，跳过非交易日。
  - 市场天气由指数涨跌本地计算，含气温和图标。
  - 默认深色模式，不提供深浅模式入口。
  - 股票代币参考 HTML logo 动画，不展示 ETF 代币。
  - 今日问候按时间段取文案，打字效果显示，无渐变。

## Android 当前状态

- Android 是原生 Java 单 Activity 手写 UI：
  - `AndroidPortfolioDashboard/app/src/main/java/com/longwishine/portfolio/MainActivity.java`
  - `AndroidPortfolioDashboard/app/src/main/AndroidManifest.xml`
  - `AndroidPortfolioDashboard/app/src/main/res/values/styles.xml`
  - 80 个股票 logo 位于 `AndroidPortfolioDashboard/app/src/main/res/drawable/logo_*.png`
- 当前包名：`com.longwishine.portfolio`
- app 名称：`浪理财`
- minSdk：23
- targetSdk：35
- 权限：仅 `android.permission.INTERNET`

## Android 已实现功能

- 四个 Tab：总览、明细、市场、数据。
- 总览：
  - 股票代币轮播。
  - 今日问候卡片。
  - 美股账户总资产、USD/CNY 切换、浮动盈亏、持仓市值、现金占比。
  - 总资产走势折线图。
- 明细：
  - 持仓明细在顶部。
  - 总资产穿透视图饼图。
  - 穿透持仓汇总，SMH / QQQM 合并同类项，显示涨跌幅和穿透金额。
  - SMH、QQQM 单独 ETF 持仓组成。
- 市场：
  - 市场气温及天气，根据指数平均涨跌和最弱指数本地计算。
  - 使用 Emoji：⛈️ / ☁️ / 🌥️ / ☀️ / 🌡️。
  - 市场指数列表。
- 数据：
  - 行情同步。
  - 行情日志最近两条。
  - 手动计算在行情日志下方。

## Android 最新一轮 UI/动画调整

- 用户指出：股票代币轮播和问候动画一样被 5 秒行情刷新重置，导致轮播卡住。
- 已修：
  - `TokenTickerView` 作为字段复用，不再每次总览 render 都新建。
  - `fetchQuotes()` 在总览页前台时不再整页 `render()`，只更新状态并保留动画。
  - 股票代币轮播改用 Android 原生 `Choreographer.FrameCallback` 跟随 VSYNC，离屏暂停、回屏继续。
  - 移除 `onDraw()` 里无限 `invalidate()` 的粗暴刷新。
  - 问候动画加 `onAttachedToWindow` / `onDetachedFromWindow` 生命周期控制，避免残留定时任务。
  - 总资产折线图标题旁补了 `MiniIconView` 原生 Canvas 图标。
  - UI 改向 Material You：暗色渐变背景、Tonal Surface、大圆角卡片、胶囊按钮、底部导航选中容器色。

## 最新 APK 校验

- 最新 APK：`/Users/longwishine/Documents/代客理财计划/AndroidPortfolioDashboard/build/PortfolioDashboard-debug.apk`
- 最近一次 SHA-256：`73501ba06ae60fa9e84fe8eb7608b4cb4ad582c4377ed077e4e33336fe85744b`
- 签名验证：通过 v1/v2/v3。
- 权限检查：仅联网权限。
- 当前机器没有连接 Android 设备或模拟器，因此未做真机/模拟器截图验证。

## 腾讯行情规则

- 抓口：`https://qt.gtimg.cn/q=...`
- 美股个股/ETF：`us` + ticker，例如：
  - `usSMH`
  - `usQQQM`
  - `usNVDA`
  - `usTSM`
  - `usMU`
- 指数：
  - `DJI` -> `usDJI`
  - `IXIC` -> `usIXIC`
  - `SPX` -> `usINX`
- 返回编码：GB18030。
- normalize 规则：
  - 去掉 `US` 前缀。
  - 去掉 `.OQ` / `.N` 等后缀。
  - `INX` 归一为 `SPX`。

## 构建 Android APK 的本地工具链

- Android SDK 安装在：`/Users/longwishine/Documents/代客理财计划/.android-sdk`
- build tools：`.android-sdk/build-tools/35.0.0`
- platform：`.android-sdk/platforms/android-35/android.jar`
- 项目没有 Gradle，当前用手动 aapt2 / javac / d8 / zipalign / apksigner 打包。

## 重要注意事项

- 不要破坏行情抓取和总资产走势迭代逻辑。
- 不要让 5 秒行情刷新重建股票代币轮播。
- 股票代币必须无限流滚动，且使用原生帧率动画。
- 用户对 UI 质感很敏感，不接受毛坯感；继续优化时要参考 iOS 截图的视觉层级，但 Android 风格要靠近 Material You。
- 用户明确希望“输出 APK 前检查是否完成所有需求”。
- 如果继续 iOS 工作，需要先打开 Xcode 项目：
  - `/Users/longwishine/Documents/代客理财计划/PortfolioDashboardApp/PortfolioDashboardApp.xcodeproj`

## 2026-06-07 macOS / iOS 最新接力记录

### macOS 版当前状态

- 新增独立 macOS SwiftUI 项目：
  - `/Users/longwishine/Documents/代客理财计划/MacPortfolioDashboard`
- macOS App 已安装到：
  - `/Applications/浪理财.app`
- 最新 DMG：
  - `/Users/longwishine/Documents/代客理财计划/MacPortfolioDashboard/dist/浪理财.dmg`
- macOS 版已从“固定仓位看板”重构为“本地交易流水驱动资产系统”。
- 支持本地记录：
  - 入金
  - 出金
  - 买入
  - 卖出
- 买入前会校验现金是否足够，现金不足不放行。
- 卖出前会校验持仓股数是否足够。
- 用户新增任意美股代码，例如 `SPCX`，系统会：
  - 先用成交价生成持仓和盈亏；
  - 自动加入腾讯行情池；
  - 尝试按 `usSPCX` 抓取行情；
  - 行情成功后更新价格、名称、类别/行业资料。
- 本地流水持久化文件在 Application Support 下，由 `PortfolioLedgerStore` 管理。
- 本地证券资料库由 `SecurityProfileStore` 管理，保存腾讯行情返回的名称、类别/行业，以及内置 ETF/成分股映射。
- 流水删除逻辑改为“作废”：
  - 作废流水划线显示；
  - 不参与资产重算；
  - 可恢复；
  - 可彻底删除。
- 恢复“手动价格”模块：
  - 支持所有当前持仓，包括新加仓股票；
  - 用于夜盘、盘前、盘后行情源延迟时的辅助估值；
  - 下一次行情成功刷新后会自动覆盖。
- 仓位百分比溢出已修：
  - 不再直接用可能为负或接近 0 的总资产做分母；
  - 改用非负现金 + 非负持仓市值作为 allocation base；
  - 展示百分比限制在 0% 到 100%。
- 客户称呼隐私化：
  - 默认称呼为 `user`；
  - 首次打开会询问“您希望我们怎么称呼你”；
  - 称呼保存到 `UserDefaults`；
  - 数据页底部保留手动修改入口。
- 所有 `李易凡` / `财富管理客户` 文案已清除。
- 行情说明统一改为：
  - `现已实验性支持美股全平台股&指盘中实时行情`
- macOS 图标改成符合 macOS 风格的圆角方形底座图标：
  - 图标生成脚本：`MacPortfolioDashboard/script/generate_app_icon.swift`
  - 打包脚本：`MacPortfolioDashboard/script/package_dmg.sh`
- 曾遇到台前调度不显示 logo：
  - 原因是 SwiftPM 资源包曾被额外放在 `.app` 根目录；
  - 已改为标准 Bundle 结构，只放在 `Contents/Resources`；
  - 启动时显式设置 `NSApp.applicationIconImage`。

### iOS / IPA 最新状态

- 已将 macOS 版上述核心客诉功能移植回 iOS 项目：
  - `PortfolioDashboardApp/PortfolioDashboardApp/Models.swift`
  - `PortfolioDashboardApp/PortfolioDashboardApp/PortfolioViewModel.swift`
  - `PortfolioDashboardApp/PortfolioDashboardApp/TencentQuoteService.swift`
  - `PortfolioDashboardApp/PortfolioDashboardApp/ContentView.swift`
- iOS 版当前也已支持：
  - 本地交易流水驱动资产；
  - 入金 / 出金 / 买入 / 卖出；
  - 买入现金校验；
  - 卖出持仓校验；
  - 新增美股代码自动加入腾讯行情池；
  - 新增代码行情成功后写入名称与类别/行业资料；
  - 手动价格模块；
  - 流水作废、恢复、彻底删除；
  - 客户称呼首次设置与数据页手动修改；
  - 仓位百分比非负归一化，避免溢出；
  - 隐私文案处理为默认 `user`。
- iOS 数据页已从旧“手动计算”替换为：
  - `交易录入`
  - `手动价格`
  - `交易流水`
  - `客户称呼`
- iOS 行情启停按钮已加入数据页。
- iOS 版 Swift 静态类型检查已通过：
  - `swiftc -typecheck -parse-as-library PortfolioDashboardApp/PortfolioDashboardApp/Models.swift PortfolioDashboardApp/PortfolioDashboardApp/PortfolioViewModel.swift PortfolioDashboardApp/PortfolioDashboardApp/TencentQuoteService.swift PortfolioDashboardApp/PortfolioDashboardApp/ContentView.swift`
- 用户曾遇到 Xcode 报错：
  - `Unable to spawn process ... swift-frontend (Resource temporarily unavailable)`
- 处理方式：
  - 清理残留进程：
    - `pkill -f swift-frontend`
    - `pkill -f ibtoold`
    - `pkill -f SourceKitService`
  - 删除本项目 DerivedData：
    - `rm -rf ~/Library/Developer/Xcode/DerivedData/PortfolioDashboardApp-*`
  - 使用完整 Xcode 路径并限制并发构建：
    - `DEVELOPER_DIR=/Users/longwishine/Downloads/Xcode.app/Contents/Developer /Users/longwishine/Downloads/Xcode.app/Contents/Developer/usr/bin/xcodebuild -project PortfolioDashboardApp/PortfolioDashboardApp.xcodeproj -scheme PortfolioDashboardApp -destination 'generic/platform=iOS Simulator' -jobs 2 COMPILER_INDEX_STORE_ENABLE=NO build`
- 上述完整 Xcode 命令已成功构建 iOS Simulator 版本：
  - `** BUILD SUCCEEDED **`
- 当前系统默认 `xcode-select -p` 仍指向：
  - `/Library/Developer/CommandLineTools`
- 因此插件 `xcodebuildmcp` 可能仍找不到完整 Xcode。后续如需插件直接构建，需要切换 developer dir，或继续用 `DEVELOPER_DIR=/Users/longwishine/Downloads/Xcode.app/Contents/Developer` 显式指定。

## 2026-06-12 macOS 最新接力记录

### 当前最新版本

- 当前重点项目：`/Users/longwishine/Documents/代客理财计划/MacPortfolioDashboard`
- 当前已安装 App：`/Applications/浪理财.app`
- 当前最新 DMG：`/Users/longwishine/Documents/代客理财计划/MacPortfolioDashboard/dist/浪理财.dmg`
- 当前 App 版本：`1.1.1`
- 当前构建号：`20260612`
- 构建方式：
  - SwiftPM macOS SwiftUI App；
  - 构建脚本：`MacPortfolioDashboard/script/build_and_run.sh`
  - 打包脚本：`MacPortfolioDashboard/script/package_dmg.sh`

### 富途内容处理结论

- 曾测试富途 OpenD / `futu-api`：
  - 可取美股个股/ETF 的盘前、盘后、夜盘相关字段；
  - 但富途接口对美股指数价格返回“不支持 US stock indices”。
- 用户随后明确要求“撤销所有富途内容”。
- 当前源码中不应再保留：
  - `Futu`
  - `富途`
  - `OpenD`
  - `futu`
  - `MarketQuoteService`
- 当前 macOS App 已回到腾讯行情作为行情源。

### 腾讯行情和行情显示修复

- 腾讯行情服务文件：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Services/TencentQuoteService.swift`
- 腾讯字段解析确认：
  - 价格：field 3
  - 今日涨跌额：field 31
  - 今日涨跌幅：field 32
  - 例如 `usMSFT` 能返回正确涨跌幅，并非腾讯源本身固定 0.00%。
- 已修复“自动腾讯行情刷新被日志显示为手动录入价格”：
  - 问题位置：`PriceInputRow`
  - 原因：联网刷新 `holding.price` 后同步到输入框文本，触发 `.onChange(of: text)`，进而调用 `updatePrice`。
  - 修复：增加 `isSyncingFromHolding`，程序同步价格时不触发手动录入逻辑。
- 已修复穿透个股涨跌幅误显示 `0.00%`：
  - 只有 `source == "腾讯行情"` 的 Quote 才展示今日涨跌幅；
  - 本地成交价、初始数据、手动价格不再被当作“今日涨跌幅”；
  - 未拿到联网行情时显示“等待行情”。

### 总资产板块更新

- 总资产区域已新增“今日盈亏”：
  - 金额 + 百分比；
  - 绿涨红跌；
  - 支持 USD / CNY 切换。
- 浮动盈亏也已补上金额：
  - 左下角账户摘要显示：金额 + 百分比 + “浮动盈亏”；
  - 分析页概览小指标显示：金额 / 百分比；
  - 总览页指标卡继续显示金额和百分比。
- 相关文件：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Stores/PortfolioViewModel.swift`
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Views/ContentView.swift`

### DeepSeek 分析 Bot

- 分析板块保留，并已接入 DeepSeek。
- DeepSeek 服务文件：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Services/DeepSeekAnalysisService.swift`
- 用户明确允许将 DeepSeek API Key 写入源码，当前服务内置 Key。
- 当前模型：
  - `deepseek-v4-flash`
- DeepSeek API 本身没有稳定官方联网搜索能力，因此当前方案是：
  - App 先抓取公开新闻/RSS摘要；
  - 再把“组合上下文 + 搜索摘要 + 用户问题”一起发给 DeepSeek。
- 大陆可用新闻源已切到 HTTPS，避免 macOS 安全策略拦截 HTTP：
  - `https://rss.sina.com.cn/finance/usstock.xml`
  - `https://rss.sina.com.cn/roll/stock/hot_roll.xml`
  - `https://rss.sina.com.cn/roll/finance/hot_roll.xml`
  - `https://finance.eastmoney.com/a/czqyw.html`
  - `https://www.cls.cn/telegraph`
- 请求头已加强：
  - Safari 风格 User-Agent；
  - `Accept-Language: zh-CN...`
  - `Accept: text/html,application/xml...`
- DeepSeek 回复已支持 Markdown 渲染：
  - Bot 回复支持标题、列表、加粗、链接等；
  - 用户输入仍按普通文本显示；
  - Bot 文本可选择复制。

### 聊天记录管理

- 分析 Bot 聊天记录已持久化。
- 存储文件由 `AnalysisMessageStore` 管理：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Stores/AnalysisMessageStore.swift`
  - 实际文件在用户 Application Support 下：`MacPortfolioDashboard/analysis-messages.json`
- 当前对话管理规则：
  - 今日消息显示在“今日对话”；
  - 上一日及更早的消息自动归档到“历史对话”；
  - 历史对话按日期分组，可展开查看；
  - 可清空今日对话，历史记录保留。
- 相关模型：
  - `AnalysisMessage`
  - `AnalysisHistoryGroup`
  - 均在 `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Models/Models.swift`

### 当前重要源码位置

- App 入口：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/App/MacPortfolioDashboardApp.swift`
- 主界面：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Views/ContentView.swift`
- 状态与资产逻辑：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Stores/PortfolioViewModel.swift`
- 交易流水存储：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Stores/PortfolioLedgerStore.swift`
- 证券资料存储：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Stores/SecurityProfileStore.swift`
- 聊天记录存储：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Stores/AnalysisMessageStore.swift`
- 腾讯行情：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Services/TencentQuoteService.swift`
- 本地兜底分析：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Services/PortfolioAnalyst.swift`
- DeepSeek 分析：
  - `MacPortfolioDashboard/Sources/MacPortfolioDashboard/Services/DeepSeekAnalysisService.swift`

### 构建与打包命令

- 快速构建：
  - `cd /Users/longwishine/Documents/代客理财计划/MacPortfolioDashboard`
  - `swift build --jobs 2`
- 打包 DMG：
  - `./script/package_dmg.sh`
- 安装并启动当前 DMG 内 App 的做法：
  - 关闭旧进程；
  - 删除 `/Applications/浪理财.app`；
  - 复制 `MacPortfolioDashboard/dist/dmg-root/浪理财.app` 到 `/Applications/浪理财.app`；
  - `open -n /Applications/浪理财.app`

### 后续注意事项

- 用户偏好：先实际验证接口/效果，再写入正式逻辑。
- 用户希望输出为非技术化表达，但新对话若继续开发仍可直接改代码、构建、打包。
- 不要重新引入富途内容，除非用户明确要求重新评估。
- DeepSeek 联网不是 DeepSeek 自身联网，而是 App 侧搜索摘要增强。
- 如果新闻摘要仍不可用，优先检查：
  - 源站 HTTPS 是否可访问；
  - App 内 URLSession 是否被 ATS/网络策略拦截；
  - 源站是否需要更完整请求头；
  - RSS 是否过旧，应增加新的大陆可用财经源。
- 用户当前最关心 macOS 版实际可见效果，改完后应构建、打包、安装并启动。

### 2026-06-14 macOS 新闻功能对话历史

- 用户要求 macOS 端新增“新闻”栏目，展示美股要闻。
- 用户指定新闻源只能围绕这三个：
  - `http://stock.10jqka.com.cn/usstock/mggsxw_list/`
  - `https://www.ushknews.com`
  - `https://wallstreetcn.com/live/us-stock`
- 抓取需求：
  - 打开应用时抓取新闻标题，按时间最新 100 条；
  - 标题下显示精确到分钟的发布时间和信源；
  - 信源显示为“同花顺 / 金十 / 华尔街见闻”；
  - 新闻前有可点击入口，可跳转浏览器原文；
  - 后台每 1 分钟刷新一次，新新闻替换旧展示；
  - 用户可点击刷新或下拉刷新；
  - 新闻标题、时间戳加入应用语料库，不能覆盖旧语料，应持续积累。
- 用户后来要求增加新闻筛选：
  - 筛选开启后，只看与当前持仓有关的新闻；
  - 筛选关键词要从本地持仓读取，包括直接持仓公司名、代码、行业；
  - ETF 穿透持仓也要纳入关键词；
  - 对本地只有代码的公司，要通过本地代码-公司名对照表辅助匹配。
- 金十抓取关键经验：
  - `https://www.ushknews.com` 的快讯接口是 `https://flash-api.ushknews.com/get_flash_list_with_channel`；
  - 必须带请求头 `x-app-id: brCYec5s1ova317e` 和 `x-version: 1.0.0`；
  - 首次请求 `max_time` 要传空值，即 `max_time=`，不要传 `0`；
  - 如果要取用户指定首页的分钟级最新流，`channel` 也传空值；
  - 只取 `channel=1` 会变成较窄的美股频道，周末可能出现大量老新闻。
- 华尔街见闻抓取关键经验：
  - 用户给的页面是 `https://wallstreetcn.com/live/us-stock`；
  - 结构化接口可用 `https://api-one-wscn.awtmt.com/apiv1/content/lives?channel=us-stock-channel&limit=100`；
  - 之前误用 `channel=us-stock` 会返回空或过少数据。
- 同花顺抓取关键经验：
  - 页面为 GBK/GB18030 编码；
  - 列表页可直接解析 `arc-title` 标题、链接和 `MM月dd日 HH:mm` 时间；
  - 当前测试能解析约 25 条当前页新闻。
- 用户明确纠正：
  - 新闻要按时间从新到旧排列；
  - 新闻文本要左对齐；
  - 如果抓取数量只有 29 条，属于异常；
  - 大部分新闻老掉牙也不接受，因为给的源是分钟级刷新的。
- 已有一次覆盖安装：
  - 覆盖到 `/Applications/浪理财.app`；
  - 旧版曾备份到 `/Applications/浪理财.app.backup-codex-20260614182233`；
  - 后续用户明确说“这次任务不用打包”，不要再自动覆盖安装。
- 用户最后要求“对话历史写进记忆库”：
  - 这里的记忆库指项目文件夹中已存在的接力知识库，不是新增 App 内部存储；
  - 不要新建 `AnalysisMemoryStore` 或单独的 `analysis-memory.json`；
  - App 现有聊天持久化仍由 `AnalysisMessageStore` 管理，实际存储在 Application Support 下的 `analysis-messages.json`。

### 腾讯行情字段记录

- 腾讯美股行情接口仍为：
  - `https://qt.gtimg.cn/q=usSPCX,usNVDA,...`
- 多代码用逗号拼接。
- 返回编码仍按 GB18030 / GBK 处理。
- 已确认腾讯字段：
  - 字段 1：短名称，例如 `英伟达`
  - 字段 2：带市场后缀代码，例如 `NVDA.OQ`
  - 字段 3：最新价
  - 字段 30：行情时间
  - 字段 31：涨跌额
  - 字段 32：涨跌幅
  - 字段 46：英文长名称
  - 字段 56：类别字段，例如 `GP`、`GP-ETF`
- 腾讯并不稳定提供所有美股的细分行业字段。
- 当前行业写入策略：
  - 腾讯类别能识别 ETF 时写为 `ETF`；
  - 内置常见股票行业映射优先；
  - ETF 成分资料可补全持仓穿透中的股票行业；
  - 冷门股没有明确行业时先写为 `美股`。

### 后续高优先级注意

- iOS 版现在已经能构建，但尚未做真机/模拟器 UI 点击流完整验收。
- 如继续 iOS，需要优先用完整 Xcode 跑：
  - 启动 App；
  - 首次称呼弹窗；
  - 新增入金；
  - 买入 `SPCX`；
  - 现金不足买入禁止；
  - 手动录入 `SPCX` 价格；
  - 流水作废/恢复/彻底删除；
  - 明细页百分比不溢出；
  - 腾讯行情刷新后 `SPCX` 是否进入持仓和行情日志。

## 2026-06-14 IPA 同步记录

### 本次已确认写入 IPA 的功能

- 总资产板块新增 `今日盈亏`：
  - 展示金额 + 百分比；
  - 金额跟随 USD/CNY 切换；
  - 今日盈利为绿色，亏损为红色；
  - 只统计已拿到 `腾讯行情` 的持仓，避免用本地价格误算。
- 自动腾讯行情刷新不再误触发 `手动录入价格` 日志：
  - iOS `PriceInputRow` 已区分程序同步价格和用户手动输入；
  - `PortfolioViewModel.updatePrice(for:text:)` 对相同价格增加保护，避免重复写入日志。
- 穿透个股涨跌幅只认 `腾讯行情`：
  - 本地成交价、初始数据、手动价格不再显示成误导性的 `0.00%`；
  - 没拿到联网行情时显示 `等待行情`；
  - iOS 已新增 `isLiveQuote(_:)` 和 `displayQuoteChange(_:)` 作为统一展示入口。

### 本次涉及的 iOS 文件

- `PortfolioDashboardApp/PortfolioDashboardApp/PortfolioViewModel.swift`
  - 新增 `todayPnl` / `todayPnlPct`；
  - 新增 `isLiveQuote(_:)` / `displayQuoteChange(_:)`；
  - 加强 `updatePrice(for:text:)`，避免自动行情同步误记手动日志。
- `PortfolioDashboardApp/PortfolioDashboardApp/ContentView.swift`
  - 总资产卡片新增 `今日盈亏` 指标；
  - ETF 组成列表和穿透持仓汇总改用 `displayQuoteChange(_:)`；
  - `PriceInputRow` 增加程序同步价格保护。

### IPA 打包方式

- 打包脚本：
  - `PortfolioDashboardApp/script/package_ipa.sh`
- 最新未签名 IPA：
  - `PortfolioDashboardApp/dist/浪理财_unsigned.ipa`
- 当前机器完整 Xcode 路径：
  - `/Users/longwishine/Downloads/Xcode.app/Contents/Developer`
- 系统默认 `xcode-select -p` 仍可能指向 CommandLineTools，所以后续 iOS 打包应继续通过脚本或显式设置：
  - `DEVELOPER_DIR=/Users/longwishine/Downloads/Xcode.app/Contents/Developer`
- 2026-06-14 已重新打包并确认：
  - IPA 时间：`2026-06-14 17:49`
  - 包内 `Payload/PortfolioDashboardApp.app/PortfolioDashboardApp` 同步为同一时间；
  - 构建输出为 `** BUILD SUCCEEDED **`。

### 后续规则

- 以后凡是给 DMG 新增资产、行情、AI 分析、Markdown 展示、来源过滤、日志去重等业务功能，都要同步检查 IPA。
- 用户明确要求：`自从上次修改 IPA 以来，为 DMG 新增的所有新功能，都应用给 IPA`。
- 输出 IPA 前必须至少检查：
  - 源码中是否有对应关键词；
  - IPA 是否为最新时间；
  - 包内 App 二进制是否同步更新；
  - 关键文案是否能在二进制或源码中查到。
