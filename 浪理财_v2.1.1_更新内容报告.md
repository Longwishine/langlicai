# 浪理财 v2.1.1 更新内容报告

日期：2026-06-17

## 本次重点

v2.1.1 修复了收盘后估值口径、今日盈亏表述和总资产走势可信度问题；优化新闻首次启动性能；新增总览页股票代币拖动和点击查价能力。macOS App 已覆盖安装，DMG 与 unsigned IPA 已重新打包。

## 行情与盈亏口径

- 收盘后的闭市时段，股票和 ETF 估值优先使用 TradingView 盘后市场最终报价；缺失时才回退常规收盘价。
- 总资产旁的非盘中估值说明细分为“非盘中交易报价核算”和“最新盘后收盘价核算”。
- 今日盈亏模块按盘中、盘前、盘后、盘后收盘、常规收盘回退和手动估值显示对应标题与比较基准。
- 修复周末或假日回退交易日时，闭市行情可能额外多回退一天的问题。

## 总资产走势

- 废弃“用当前价格反推过去 5 个交易日”的旧逻辑。
- 只在直接持仓行情完整、行情时间属于有效美股交易日时记录一个真实总资产快照。
- 同一交易日多次刷新只替换当天点位；新交易日追加点位；旧交易日或非交易日数据不会覆盖趋势。
- 趋势数据已本地持久化，重启后继续沿用历史有效点位。
- 图表口径改为“近 5 个有效行情日”。

## 新闻性能

- 首次启动不再自动进入新闻抓取循环，避免新闻网络请求和筛选任务拖慢总览首屏。
- 新闻页进入时按需刷新，并保留手动下拉/按钮刷新能力。
- 新闻抓取后先做语料库去重，只对新增新闻条目做一次持仓关键词白名单筛选。
- 持仓相关新闻继续写入独立归档，现有持仓筛选、历史语料库和 AI 新闻摘要能力不变。

## 股票代币交互

- 首页股票代币轮播行支持鼠标和手指横向拖动。
- 点击单个代币会打开小型二级菜单，并自动查询对应股票代码。
- 菜单展示最新价格、涨跌幅、行情源和所属行业。
- 新增本地代币代码库，覆盖 `GOOG`、`META`、`TSLA`、`WMT`、`PANW`、`PLTR`、`SHOP`、`SPACEX`，其中 `SPACEX` 映射为 `SPCX`。
- 对缺少公开股票代码的代币，菜单明确提示缺少代码，不伪造行情。

## 验证与产物

- Mac 测试：`swift test -Xswiftc -gnone` 通过 11 项。
- iOS Release 构建：`xcodebuild ... CODE_SIGNING_ALLOWED=NO build` 通过。
- macOS 覆盖安装：`/Applications/浪理财.app`，版本 `2.1.1`，构建 `20260617`。
- DMG：`MacPortfolioDashboard/dist/浪理财-2.1.1.dmg`
- IPA：`PortfolioDashboardApp/dist/浪理财-2.1.1_unsigned.ipa`
